import 'package:flutter/material.dart';

class OnsalePage extends StatelessWidget {
  const OnsalePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('On Sale Page')));
  }
}
